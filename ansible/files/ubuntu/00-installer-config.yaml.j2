network:
  ethernets:
    ens160:
      dhcp4: false
      addresses:
        - {{ lan_ip }}/24
      nameservers:
        addresses: [{{ name_server }}]
      routes:
        - to: default
          via: {{ gateway }}
  version: 2
      